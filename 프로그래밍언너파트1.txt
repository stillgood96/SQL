IDE 란? 
통합개발환경
프로그램 개발과 관련
컴파일,링크가 자동으로 됨
IDE가 없을시 불편하지만 개발 가능

기계어로 번역후 라이브러리와 연결해서
실행파일로 만드는 것 => link

한 줄 주석 => //

int a = 3;
printf(" %d ", a);   정수출력시 %d 사용

a = 7, b = 3
a++       : 8 (단독일때는 ++a, a++)
++a + ++b : 9 + 4  : 13

switch(a) {   
}
a => 상수, 정수, 수식

switch 구문에서 분기를 나갈때 사용하는 명령
=> break

return 3.14f;
다음 코드에서 return되는 데이터형은? float

return 3.14;
다음 코드에서 return되는 데이터형은? double

              행  행 행 
int a[][4] = {{},{},{}}
몇행몇열 배열인가?  3x4

"hello, world'를 저장하려고 문자배열 선언
=> char str[12+1];   (종결문자 \n도 포함)

포인터변수의 크기는 데이터유형에
상관없이 4바이트를 차지!

정수형 포인터변수의 주소를 보관하는
변수 pt를 선언하는 문장
int **pt;


[!][][][] 에서 첫번째 요소를 가리키는 코드는
arr + 1
&arr[1]
arr++
arr[1]    arr 배열의 두번째 요소를 지칭

struct car {
   int size;
   int weight;
};

구조체 선언시 struct 구조체명 변수명;
struct car one;

레지스터 변수 : 
레지스터 내에 선언하는 변수 (처리속도빠름)
레지스터 변수는 지역변수로만 사용가능

int sub(){
   static int a=0;  // 전역변수
   a++;
   rerurn a*a;
}
2번 호출시 결과값은? 4

int (*p)[7];로 선언시 열 크기는? 
4 * 7 => 7

계속해서 프로그램을 수행하고자 할때 사용하는 구문
=> 반복문

매크로 상수 정의시 사용하는 키워드
=> define

표준 문자열 함수로 자동줄바꿈이 처리되는 함수
=> puts     









